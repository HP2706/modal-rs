[package]
name = "modal-proto"
version = "0.1.0"
edition = "2021"

[dependencies]
prost = "0.13"
prost-types = "0.13"
tonic = { version = "0.12", features = ["transport", "tls"] }

[build-dependencies]
tonic-build = "0.12"
prost-build = "0.13"
